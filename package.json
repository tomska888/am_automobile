{
    "name": "am-automobile-monorepo",
    "version": "1.0.0",
    "private": true,
    "description": "AM Automobile — Full Stack Car Dealership Website (Monorepo)",
    "scripts": {
        "dev": "concurrently --kill-others-on-fail --names \"CLIENT,SERVER\" --prefix-colors \"cyan,yellow\" \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
        "setup": "npm install --prefix client && npm install --prefix server",
        "build": "npm run build --prefix client",
        "start": "npm run start --prefix server",
        "lint": "npm run lint --prefix client"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "engines": {
        "node": ">=18.0.0",
        "npm": ">=9.0.0"
    }
}